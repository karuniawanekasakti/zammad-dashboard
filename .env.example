# Set to "true" to use real FastAPI backend instead of mock data
VITE_USE_BACKEND=false

# Override API base URL (defaults to /api/v1 which proxies via vite)
# VITE_API_BASE=/api/v1

# Override WebSocket base URL
# VITE_WS_BASE=ws://localhost:8000/api/v1
